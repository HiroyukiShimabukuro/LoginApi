services:
  database:
    image: "postgres:16.2-alpine3.19"
    container_name: postgres
    restart: always
    env_file:
      - ../../.env
    ports:
      - 5431:5432
    volumes:
      - pgdata:/data/postgres
  app:
    build: ../../
    container_name: loginApi
    ports:
      - 3333:3333
    env_file:
      - ../../.env
    links:
      - database
    depends_on:
      - database

volumes:
  pgdata:
    driver: local
